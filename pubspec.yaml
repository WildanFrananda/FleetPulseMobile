name: fleet_pulse_mobile
description: "FleetPulse Driver — telemetry & orders terminal."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  provider: ^6.1.5
  get_it: ^9.2.1
  injectable: ^3.0.0
  freezed_annotation: ^3.0.0     # <-- major 3
  json_annotation: ^4.9.0

  web_socket_channel: ^3.0.3
  geolocator: ^14.0.2
  permission_handler: ^12.0.1
  flutter_secure_storage: ^10.3.1
  flutter_foreground_task: ^10.0.0
  url_launcher: ^6.3.2
  dio: ^5.11.0
  retrofit: ^4.9.2
  clock: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  injectable_generator: ^3.0.2
  freezed: ^3.0.6                 # <-- major 3
  json_serializable: ^6.9.5
  retrofit_generator: ^10.2.8
  mocktail: ^1.0.5
  fake_async: ^1.3.3
  integration_test:
    sdk: flutter


flutter:
  uses-material-design: true
